server:
  port: 8002
spring:
  jpa:
    database: postgresql
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQL10Dialect
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5439/securdb
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
  application:
    name: hotelWeb-app
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

